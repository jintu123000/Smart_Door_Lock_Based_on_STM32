# 2026-03-25 工作日志

## 生成 FreeRTOS+HAL 版智能门锁代码

- 读取原工程所有 APP/ 模块源码（main.c, ui.c, bsp_rc522.c, bsp_uart.c, dht11.c, matrix_button.c, Servo.c, PWM.c, BUZZER.c, timer.c）
- 基于原工程结构，生成 FreeRTOS + STM32F1xx HAL 重构版本
- 输出目录：`C:\Users\24263\Desktop\SmartLock_FreeRTOS\`（共 28 个文件）

### 目录结构
- `Inc/`：bsp_config.h（引脚宏）、FreeRTOSConfig.h（内核配置）
- `Drivers/`：bsp_uart, bsp_buzzer, bsp_dht11, bsp_rc522, bsp_matrix_key, bsp_servo, bsp_oled
- `App/`：main.c, app_shared.h/.c（共享对象：队列/互斥量/软件定时器）
- `Tasks/`：task_key, task_dht11, task_rfid, task_ui

### 核心架构变更
- TIM2 中断调度 → FreeRTOS 软件定时器（150ms/800ms/5s）
- 全局标志位 → xTaskNotifyGive/ulTaskNotifyTake
- 全局变量 → 结构体 + 互斥量（gSensor/gDoor/gUI）
- DHT11 临界区保护替代原"关TIM2中断"
- DWT 微秒延时（DHT11 + RC522 共用）
- OLED bsp_oled.c 内置 ASCII 8x16 字库 + 中文 12x16 字库
